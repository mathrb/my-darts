// Database Factory Stub
// Fallback that satisfies the type checker.
// Never actually called at runtime.

import 'package:drift/drift.dart';

Future<QueryExecutor> createDatabaseExecutor() async {
  throw UnsupportedError(
    'createDatabaseExecutor() called in stub — '
    'check your conditional import setup.',
  );
}
