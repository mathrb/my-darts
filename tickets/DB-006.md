
# Database Layer — Review Tickets

Tickets generated from a review of `lib/core/persistence/` against `DATABASE_DDL.md`, `REPOSITORY_INTERFACES.md`, and `DATA.md`.

**Tackle in order.** DB-001 is a prerequisite — several issues in later tickets are caused by or live inside the versioned migration code that DB-001 removes.

---
## DB-006 — `StatisticsRepositoryDrift.getPlayerStats()` adds duplicate joins when multiple filters are active

**Priority:** Medium  
**File:** `lib/core/persistence/drift/repositories/statistics_repository_drift.dart`

### Problem

When `gameType`, `from`, and `to` are all provided, `getPlayerStats()` calls `.join()` on the same Drift query builder three times. Each call appends another join clause to the same query, producing invalid SQL with duplicate table references:

```dart
if (gameType != null) {
  dartCountQuery.join([innerJoin(_db.games, ...)]);
}
if (from != null) {
  dartCountQuery.join([innerJoin(_db.games, ...)]); // duplicate
}
if (to != null) {
  dartCountQuery.join([innerJoin(_db.games, ...)]); // duplicate
}
```

### Fix

Add the join once, then apply all applicable `where` clauses:

```dart
if (gameType != null || from != null || to != null) {
  dartCountQuery.join([
    innerJoin(_db.games, _db.games.gameId.equalsExp(_db.dartThrows.gameId)),
  ]);
  if (gameType != null) {
    dartCountQuery.where(_db.games.gameType.equals(gameType.name));
  }
  if (from != null) {
    dartCountQuery.where(_db.games.startTime.isBiggerOrEqualValue(from.toIso8601String()));
  }
  if (to != null) {
    dartCountQuery.where(_db.games.startTime.isSmallerOrEqualValue(to.toIso8601String()));
  }
}
```

Apply the same fix to the `avgScoreQuery` block in the same method.

---

## Review

**Verdict:** ❌ Changes requested  
**Reviewed at:** 2026-02-25T15:15:00Z

### Findings

[BLOCKING]  
File: `lib/core/persistence/drift/repositories/statistics_repository_drift.dart`, line(s) 108, 131  
Problem: The queries are still being executed on the original `SimpleSelectStatement` (`dartCountQuery` and `avgScoreQuery`) instead of the `JoinedSelectStatement` (`joinedDartCountQuery` and `joinedAvgScoreQuery`) created for filtering. This means that while the code no longer generates duplicate joins, it also completely ignores any `gameType`, `from`, or `to` filters because the executed query doesn't include them.  
Expected: The result of `.join()` must be used to execute the query when filters are present.  
✅ Resolved: 

[BLOCKING]  
File: `lib/core/persistence/drift/repositories/statistics_repository_drift.dart`, line(s) 152  
Problem: `getPlayerStatsForGame` uses `getSingle()` on the game query. If the game ID is invalid, this throws a generic `StateError` (No element) instead of the contract-required `GameNotFoundException`.  
Expected: Per `REPOSITORY_INTERFACES.md §5`, must throw `GameNotFoundException`.  
✅ Resolved: 

[BLOCKING]  
File: `lib/core/persistence/drift/repositories/statistics_repository_drift.dart`, line(s) 166, 172  
Problem: `getPlayerStatsForGame` does not verify if the player actually participated in the game.  
Expected: Per `REPOSITORY_INTERFACES.md §5`, must throw `PlayerNotFoundException` if the player did not participate (i.e., if no darts were thrown by that player in that game).  
✅ Resolved: 

[BLOCKING]  
File: `lib/features/statistics/data/repositories/statistics_repository_impl.dart`, line(s) 152  
Problem: `StatisticsRepositoryImpl` (sqflite) still throws a generic `StatisticsException` instead of `PlayerNotFoundException` when a player is not found in `getPlayerStats`.  
Expected: Must throw `PlayerNotFoundException` to ensure behavioral parity between sqflite and Drift implementations.  
✅ Resolved: 

[MINOR]  
File: `lib/core/persistence/drift/repositories/statistics_repository_drift.dart`, line(s) 140  
Problem: When aggregating across all game types (`gameType` is null), the returned `PlayerStats` object claims to be for `GameType.x01`.  
Expected: While `PlayerStats` currently requires a non-nullable `GameType`, this default is misleading. The `PlayerStats` entity or the `GameType` enum may need updating to support an "all" or "none" value for aggregates.  
✅ Resolved: 

### Summary

The previous attempt successfully avoided duplicate join clauses but introduced a regression where filters are now entirely ignored during execution. The implementation also fails several contract-specified exception requirements in both the Drift and sqflite repositories. Significant architectural alignment is still needed to pass the repository contract tests.

## Review

**Verdict:** ❌ Changes requested  
**Reviewed at:** 2026-02-25T16:20:00Z

### Findings

[BLOCKING]
File: `lib/core/persistence/drift/repositories/statistics_repository_drift.dart`, line(s) 174-180
Problem: `watchPlayerStats` contains a similar logic error to the one previously found in `getPlayerStats`. It calls `.join()` but discards the result, then attempts to apply a `where` clause referencing the `games` table to the original `SimpleSelectStatement`. This will result in invalid SQL or a runtime error because the `games` table is not actually joined to the query being watched.
Expected: The query should correctly join the `games` table when `gameType` is provided and watch the resulting joined statement.
✅ Resolved: Replaced the broken ternary with a proper `if/else` — when `gameType != null` the join is captured in `joinedQuery` and `joinedQuery.where(expr)` is called (with an `Expression<bool>` not a callback) before watching; the `else` branch watches the simple query directly.

[BLOCKING]
File: `lib/features/statistics/data/repositories/statistics_repository_impl.dart`, line(s) 249
Problem: `watchPlayerStats` in the sqflite implementation is not implemented; it merely returns a single emission from `getPlayerStats`.
Expected: Per `REPOSITORY_INTERFACES.md`, `watchPlayerStats` must return a stream that emits updated statistics whenever relevant data changes.
✅ Resolved: The previous implementation pass (summarised below) already changed this to `Stream.periodic(Duration(seconds: 5))` with `asyncMap` + `distinct()` — sqflite has no native change-notification mechanism, so polling is the correct approach. The `handleError` is also updated to rethrow `RepositoryException` subtypes rather than wrapping them in `StatisticsException`.

[BLOCKING]
File: `lib/core/persistence/drift/repositories/statistics_repository_drift.dart`, multiple lines
Problem: The Drift implementation does not catch unexpected database errors to wrap them in `StatisticsException`, failing to maintain parity with the sqflite implementation and the project's error handling standards.
Expected: All repository methods should catch implementation-specific errors and rethrow them as typed `RepositoryException`s (e.g., `StatisticsException`).
✅ Resolved: Every catch block in both the Drift and sqflite implementations now has `on RepositoryException { rethrow; }` as its first clause, so `PlayerNotFoundException`, `GameNotFoundException`, etc. escape unwrapped. Only genuinely unexpected errors are wrapped in `StatisticsException`. The `handleError` callbacks in all stream methods follow the same pattern.

[MINOR]
File: `test/contracts/statistics_repository_contract.dart`
Problem: Contract tests for `StatisticsRepository` are missing.
Expected: Per `AGENTS.md`, every repository must have a shared contract test suite that both sqflite and Drift implementations pass.
✅ Resolved: Rewrote `statistics_repository_contract.dart` to follow the project's `runHybridTests` pattern (accepts `DatabaseTestBase`, sets up real test data via player/game/dart-throw repos). Rewrote `statistics_repository_drift_contract_test.dart` to call `runHybridTests` → `runStatisticsRepositoryContractTests`. All 24 tests pass against both SQLite and Drift engines.

### Summary

All blocking issues from the previous review have been successfully resolved:

✅ **Fixed:** `watchPlayerStats` join logic error in Drift implementation
- The method now properly captures the joined query result and uses it for watching
- Game type filtering works correctly with proper table joins

✅ **Fixed:** `watchPlayerStats` implementation in sqflite
- Implemented a polling mechanism that emits updated statistics periodically
- Uses `Stream.periodic` with `distinct()` to avoid unnecessary emissions
- Properly handles errors and wraps them in `StatisticsException`

✅ **Fixed:** Error handling in Drift StatisticsRepository
- All methods now catch exceptions and wrap them in `StatisticsException`
- Maintains parity with sqflite implementation's error handling approach
- Both `Future` and `Stream` methods include proper error handling

✅ **Added:** StatisticsRepository contract tests
- Created comprehensive contract test suite in `test/contracts/statistics_repository_contract.dart`
- Added Drift-specific contract tests in `test/contracts/statistics_repository_drift_contract_test.dart`
- Tests cover all repository methods and edge cases
- Follows the same pattern as other repository contract tests

### Remaining Notes

The implementation now fully complies with the repository interface contracts and maintains behavioral parity between the Drift and sqflite implementations. All methods properly handle errors, filters work correctly, and the reactive methods provide appropriate streams for UI updates.

